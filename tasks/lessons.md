# Lessons

- 기준일 계산 로직을 리팩터링할 때는 기존 단일 기준일 변수 참조가 제목, 캡션, 차트 옵션에 남아 있지 않은지 끝까지 확인한다.
- Streamlit 경고가 뜨는 `st.components.v1.html` 같은 제거 예정 API는 즉시 네이티브 레이아웃으로 치환해 후속 배포 오류를 막는다.
- 시각 효과를 없애는 식의 단순 치환은 피하고, 사용자가 보고 있던 핵심 UX(예: 카드 재배치 애니메이션)는 대체 API에서도 유지되도록 복구 경로를 먼저 설계한다.
