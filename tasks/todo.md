# 작업 계획

- [x] `000688.SS` 다운로드 실패 재현 경로와 실제 심볼 유효성 확인
- [x] 개별 심볼 실패가 콘솔을 오염시키지 않도록 데이터 조회 경로 보강
- [x] `pd.concat` 정렬 경고 제거
- [x] 검증 수행 및 결과 기록
- [ ] git 점검 후 커밋/푸시

- [x] 기준일 선택 및 데이터 로드 범위 문제 원인 분석
- [x] 과거 날짜 선택 허용 및 인덱스별 기준가 계산 로직 수정
- [x] 검증 수행 및 결과 기록
- [x] 회귀 오류 및 Streamlit 경고 수정
- [x] 카드 재배치 애니메이션 복구
- [x] 차트 가로 폭 확장
- [x] 카드 폭 원복 및 차트 단독 확장
- [x] git 점검 후 커밋/푸시

# 리뷰

- 원인 3: `000688.SS`는 현재도 Yahoo Finance에 존재하는 유효 심볼인데, `yfinance.download()`가 출시 전 구간까지 장기간 조회할 때 빈 응답과 `possibly delisted` 로그를 간헐적으로 내고 있었음
- 수정: `000688.SS`는 실제 지수 출시일인 `2020-07-23` 이후만 요청하도록 시작일을 보정
- 수정: `yfinance.download()`가 비어 있을 때 `Ticker.history()`로 한 번 더 시도하고, 실패 로그는 앱 콘솔에 반복 노출되지 않도록 흡수
- 수정: `pd.concat(..., sort=False)`를 명시한 뒤 `sort_index()`를 유지해 pandas 정렬 경고 제거
- 검증: 웹 확인 기준 `000688.SS`는 Yahoo Finance에 현재도 노출되는 유효 심볼이므로 상장폐지 심볼로 교체할 사안은 아님
- 검증: `python3 -m py_compile app.py` 통과

- 원인 1: 데이터 조회 시작일이 `전년도 1월 1일`로 고정돼 있어 `2025-01-01` 선택 시 직전 거래일(`2024-12-31`) 보정이 불가능했음
- 원인 2: 기준가를 전체 지수에 대해 단일 날짜 행으로 잡아 해당 날짜에 휴장인 시장은 `NaN`이 되어 차트/카드에서 제외됐음
- 수정: 전체 히스토리를 불러오고, 각 지수별 `선택일 이전 마지막 유효 종가`를 기준가로 사용하도록 변경
- 수정: 날짜 입력 최소값을 넓혀 과거 날짜도 선택 가능하게 변경
- 수정: 리팩터링 후 남아 있던 `effective_base_timestamp` 참조 제거
- 수정: `st.components.v1.html` 대신 Streamlit 네이티브 컬럼 카드 레이아웃으로 교체
- 수정: 인덱스별 개별 다운로드로 바꿔 일부 심볼 실패가 전체 조회 오류처럼 보이지 않게 완화
- 수정: 카드 영역을 `st.html` 기반 HTML/JS 렌더링으로 바꿔 수익률 순서 재배치 애니메이션 복구
- 수정: 페이지 컨테이너 최대 폭을 `1000px`에서 `1240px`로 늘려 `Index Performance` 차트 가로 영역 확장
- 수정: 페이지 기본 폭은 `1000px`로 되돌리고 차트만 full-width 래퍼로 `1240px`까지 확장
- 검증: `python3 -m py_compile app.py` 통과
- 검증 한계: 현재 쉘 환경에 `pandas`, `streamlit` 등 런타임 의존성이 없어 실제 데이터 조회 기반 실행 검증은 미실행
